# This workflow will install Python dependencies and run tests with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Conversion-container pytests

on:
  push:
    branches: ["gcp-cd-arxiv-development"]
  pull_request:
    branches: ["gcp-cd-arxiv-development"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      LATEXML_OXIDE_VERSION: "0.7.6"
      TMPDIR: /tmp
    steps:
      - name: Install latexml-oxide
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # The .deb is a self-contained binary; apt pulls its texlive + graphics
          # dependencies (texlive-latex-base/-extra/-science, ghostscript,
          # imagemagick, poppler-utils, mupdf-tools, dvipng, dvisvgm).
          # Hardened against GitHub-runner flakiness: DEBIAN_FRONTEND=noninteractive
          # so no debconf prompt can hang the job, and DPkg::Lock::Timeout so we wait
          # out the runner's background unattended-upgrades holding the dpkg lock
          # instead of stalling. --no-install-recommends keeps it lean (the tests
          # only exercise plain-TeX conversions).
          sudo apt-get -o DPkg::Lock::Timeout=600 update
          curl -fsSL -o /tmp/latexml-oxide.deb \
            "https://github.com/dginev/latexml-oxide/releases/download/${LATEXML_OXIDE_VERSION}/latexml-oxide_${LATEXML_OXIDE_VERSION}-1_amd64.deb"
          sudo apt-get -o DPkg::Lock::Timeout=600 install -y --no-install-recommends /tmp/latexml-oxide.deb
          latexml_oxide --version

      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          cd conversion-container
          poetry install --all-extras
      - name: Test with pytest
        run: |
          cd conversion-container
          poetry run pytest
