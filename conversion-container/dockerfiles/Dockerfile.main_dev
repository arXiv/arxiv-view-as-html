# syntax=docker/dockerfile:1

FROM gcr.io/arxiv-development/ar5ivist-base:latest

######## Install Python 3.11 ########
RUN apt-get update -y
RUN apt-get install -y software-properties-common
RUN add-apt-repository ppa:deadsnakes/ppa
RUN apt-get update -y
RUN apt-get install -y python3.11 python3-pip

########## Everything Else ##########

WORKDIR /arxiv

RUN apt-get install -y python3.11-dev pkg-config default-libmysqlclient-dev build-essential

RUN python3.11 -m pip install cffi poetry
RUN poetry config virtualenvs.create false

COPY . .
RUN apt remove python3-blinker -y
RUN poetry install

RUN mkdir /arxiv/extracted
RUN mkdir /arxiv/publish
RUN mkdir /arxiv/locks

CMD ["gunicorn", "--bind", "0.0.0.0:8000", "-t", "600", "-w", "2", "--threads", "4", "entry_point:app"]
